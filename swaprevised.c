/*
	Revised SWAP function with two pointers
*/

#include <stdio.h>
#include <stdlib.h>

int main(int argc, char * argv[]) {
	int first;
	int second;

	return EXIT_SUCCESS;
}

void swap(int *first, int *second) {
	int temp = *first;
	*first = *second
	*second = temp;
}